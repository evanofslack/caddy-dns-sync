syncInterval: 30s
statePath: "/data/sync-state.db"
caddy:
  adminUrl: "http://caddy:2019"
dns:
  provider: "cloudflare"
  zones: ["eslack.net"]
  token: "" # Use CLOUDFLARE_API_TOKEN environment variable
  ttl: 300
reconcile:
  dryRun: true  # Safety first for development
  protectedRecords:
    - "example.eslack.com"
